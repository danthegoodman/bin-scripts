x() { source "$HOME/prog/equis/equis.sh" "$@"; }

all_flag=""
[[ $* == *--all* ]] && all_flag="--all"
x list $all_flag | while read p; do
  (
    cd "$p"
    echo "---$(basename "$p")---"
    git fetch
  )
done
